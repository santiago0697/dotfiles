# dotfiles — legacy/zsh-iTerm2

> **DEPRECATED BRANCH.** This branch archives the previous terminal stack and is
> no longer in active use. It is kept only for reference / rollback.

## Archived stack

- **Terminal:** iTerm2 + `OneHalfDark.itermcolors` preset (background image blend)
- **Shell:** zsh (`~/.zshrc`, `~/.zshenv`, `~/.zprofile`)
- **Prompt:** Oh My Zsh + Powerlevel10k rainbow (`~/.p10k.zsh`)
- **Tmux**: `~/.tmux.conf` (catppuccin theme, TPM plugins)

The active configuration lives on `master` (Ghostty + fish + starship).
`lofi-wp.png` is shared and kept in both branches.